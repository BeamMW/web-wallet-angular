import { FeeOnlyDirective } from './fee-only.directive';

describe('FeeOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new FeeOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
