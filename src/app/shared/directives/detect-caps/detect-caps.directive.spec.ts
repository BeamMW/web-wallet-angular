import { DetectCapsDirective } from './detect-caps.directive';

describe('DetectCapsDirective', () => {
  it('should create an instance', () => {
    const directive = new DetectCapsDirective();
    expect(directive).toBeTruthy();
  });
});
